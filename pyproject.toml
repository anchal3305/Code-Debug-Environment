[project]
name = "code-debug-env"
version = "0.1.0"
description = "OpenEnv Code Debugging Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv>=0.1.13"
]

[project.scripts]
server = "server:main"